server:
  port: 8080
  error:
    include-message: always

spring:
  application:
    name: insurance-manager

  # MongoDB Configuration
  data:
    mongodb:
      uri: "${MONGO_URI:mongodb+srv://atharvashinde811_db_user:jUfWnUzeVGpmiqQg@cluster1.hygoiiw.mongodb.net/insurance_db?appName=Cluster1}"

  # File Upload Limits
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  # Thymeleaf
  thymeleaf:
    cache: false # Disable for development
    mode: HTML

  # OAuth2 Configuration (Google)
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: lfvlglss35j3l7cehpd4a1fgptc5tais.apps.googleusercontent.com
            client-secret: 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
            scope:
              - openid
              - profile
              - email
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
            client-name: Google
        provider:
          google:
            authorization-uri: https://accounts.google.com/o/oauth2/v2/auth
            token-uri: https://oauth2.googleapis.com/token
            user-info-uri: https://www.googleapis.com/oauth2/v3/userinfo
            user-name-attribute: sub

# Custom Storage Configuration
storage:
  location: upload-dir

# Logging
logging:
  level:
    root: INFO
    com.insurance.manager: DEBUG
    org.springframework.security: INFO
